let expect = require('Chai').expect;
let request = require('supertest')(server);
let server = '../server';

//knex

describe('Teachers', () => {
  describe('Auth', () => {
    //log in existing teacher
    it('should return a teacher object when signing in', () => {

    })
    //sign up new teacher
    it('should create a return a teacher object when signing up', () => {

    })
  })
})
